<?php

return [
    // Default token length, if not specified on generation.
    'token_length' => 60,
    // Always set the newly created api token to be the primary one for that model.
    'make_primary' => true,
];